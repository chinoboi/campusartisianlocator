// This is a placeholder for your school logo
// Replace this with your actual school logo image file.
// You have two easy options to use your university logo in the app:

// 1) Local asset (recommended for offline use)
// - Place your image at: mobile/assets/school-logo.png
// - Import it where you render the logo and pass as `source`:
//
//   import schoolLogo from '../assets/school-logo.png';
//   <SchoolLogo source={schoolLogo} />
//
// Note: Metro bundler requires the file to exist at build time for a static
// `require`/import. If you add the file, the app will pick it up immediately.

// 2) Remote URL (configure via app.json / expo.extra)
// - Add this to `mobile/app.json` (or the project root `app.json`) under expo.extra:
//
//   {
//     "expo": {
//       "extra": {
//         "SCHOOL_LOGO_URI": "https://example.com/path/to/logo.png"
//       }
//     }
//   }
//
// The app now reads `SCHOOL_LOGO_URI` from the Expo config and displays it
// automatically in the header. This is useful if you prefer hosting the image.
